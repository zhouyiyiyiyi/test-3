version: v2.0

on:
  mr:
    tags-ignore:
      - tag_*
    users-ignore:
      - yongyiduan
 
stages:
- jobs:
    all-job-0:
      steps:
      - run: |
          echo "::set-variable name=EVENT::${{ci.event}}"