version: v2.0

on:
  mr:
    tags:
      - tag_*
    from-branches:
      - main
    users:
      - yongyiduan
 
stages:
- jobs:
    all-job-0:
      steps:
      - run: |
          echo "::set-variable name=EVENT::${{ci.event}}"