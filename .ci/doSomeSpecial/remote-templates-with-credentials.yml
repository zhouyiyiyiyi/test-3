version: v2.0
 
resources:
  repositories:
    - repository: 520795337
      name: repoA
      credentials: # 可选，默认为开启CI的用户
        personal-access-token: GIT_ACCESS_TOKEN

extends:
  template: all-test/all-extends.yml@repoA
  parameters:
    content: 执行成功